<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
</head>
<body>
    <div><%= message %></div>
    <ul>
        <% item.forEach(function(i){ %>
            <li><%= i %></li>
        <% }) %>
    </ul>
</body>
</html>
